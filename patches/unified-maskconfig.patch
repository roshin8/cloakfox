--- a/b/dom/base/AudioFingerprintManager.cpp	2026-04-17 18:44:43
+++ b/dom/base/AudioFingerprintManager.cpp	2026-04-17 21:23:31
@@ -26,24 +26,13 @@
 
 /* static */ void
 AudioFingerprintManager::SetSeed(uint32_t aUserContextId, uint32_t aSeed) {
-  nsString key;
-  key.AppendPrintf("audioFingerprintSeed_%u", aUserContextId);
-  RoverfoxStorageManager::PutUint(key, aSeed);
+  ::CloakConfigOverlay_MergeUint(aUserContextId, "audio:seed", aSeed);
   DisableFunction(aUserContextId);
 }
 
 /* static */ uint32_t
 AudioFingerprintManager::GetSeed(uint32_t aUserContextId) {
-  nsString key;
-  key.AppendPrintf("audioFingerprintSeed_%u", aUserContextId);
-  uint32_t seed = 0;
-  if (RoverfoxStorageManager::GetUint(key, seed) && seed != 0) {
-    return seed;
-  }
-  // Fallback to CLOAK_CONFIG for Workers in separate processes
-  if (auto val = MaskConfig::GetUint32("audio:seed")) {
-    return val.value();
-  }
+  if (auto val = MaskConfig::GetUint32("audio:seed")) return val.value();
   return 0;
 }
 
--- a/b/dom/base/CanvasFingerprintManager.cpp	2026-04-17 18:44:43
+++ b/dom/base/CanvasFingerprintManager.cpp	2026-04-17 21:23:58
@@ -17,22 +17,11 @@
 }
 
 void CanvasFingerprintManager::SetSeed(uint32_t aId, uint32_t aSeed) {
-  nsAutoString key;
-  key.AppendPrintf("canvasSeed_%u", aId);
-  RoverfoxStorageManager::PutUint(key, aSeed);
+  ::CloakConfigOverlay_MergeUint(aId, "canvas:seed", aSeed);
 }
 
 uint32_t CanvasFingerprintManager::GetSeed(uint32_t aId) {
-  nsAutoString key;
-  key.AppendPrintf("canvasSeed_%u", aId);
-  uint32_t seed = 0;
-  if (RoverfoxStorageManager::GetUint(key, seed) && seed != 0) {
-    return seed;
-  }
-  // Fallback to CLOAK_CONFIG for Workers in separate processes
-  if (auto val = MaskConfig::GetUint32("canvas:seed")) {
-    return val.value();
-  }
+  if (auto val = MaskConfig::GetUint32("canvas:seed")) return val.value();
   return 0;
 }
 
--- a/b/dom/base/FontSpacingSeedManager.cpp	2026-04-17 18:44:43
+++ b/dom/base/FontSpacingSeedManager.cpp	2026-04-17 21:24:05
@@ -29,32 +29,21 @@
 
 /* static */ void
 FontSpacingSeedManager::SetSeed(uint32_t userContextId, uint32_t seed) {
-  nsString key = KeyForUserContext(userContextId);
-  RoverfoxStorageManager::PutUint(key, seed);
-  
+  ::CloakConfigOverlay_MergeUint(userContextId, "fonts:spacing_seed", seed);
+
   // Mark the function as disabled for this context after first use
   DisableFunction(userContextId);
 }
 
 /* static */ uint32_t
 FontSpacingSeedManager::GetSeed(uint32_t userContextId) {
-  nsString key = KeyForUserContext(userContextId);
-  uint32_t seed = 0;
-  if (RoverfoxStorageManager::GetUint(key, seed) && seed != 0) {
-    return seed;
-  }
-  // Fallback to CLOAK_CONFIG for Workers in separate processes
-  if (auto val = MaskConfig::GetUint32("fonts:spacing_seed")) {
-    return val.value();
-  }
+  if (auto val = MaskConfig::GetUint32("fonts:spacing_seed")) return val.value();
   return 0;
 }
 
 /* static */ bool
 FontSpacingSeedManager::HasSeed(uint32_t userContextId) {
-  nsString key = KeyForUserContext(userContextId);
-  uint32_t seed = 0;
-  return RoverfoxStorageManager::GetUint(key, seed);
+  return MaskConfig::GetUint32("fonts:spacing_seed").has_value();
 }
 
 /* static */ bool
--- a/b/dom/base/ScreenDimensionManager.cpp	2026-04-17 18:44:43
+++ b/dom/base/ScreenDimensionManager.cpp	2026-04-17 21:24:23
@@ -6,6 +6,7 @@
 
 #include "ScreenDimensionManager.h"
 #include "RoverfoxStorageManager.h"
+#include "MaskConfig.hpp"
 #include "nsGlobalWindowInner.h"
 #include "xpcpublic.h"
 #include "mozilla/dom/BrowsingContext.h"
@@ -29,22 +30,15 @@
 
 /* static */ void
 ScreenDimensionManager::SetDimensions(uint32_t aUserContextId, int32_t aWidth, int32_t aHeight) {
-  nsString widthKey, heightKey;
-  widthKey.AppendPrintf("screenWidth_%u", aUserContextId);
-  heightKey.AppendPrintf("screenHeight_%u", aUserContextId);
-  RoverfoxStorageManager::PutUint(widthKey, static_cast<uint32_t>(aWidth));
-  RoverfoxStorageManager::PutUint(heightKey, static_cast<uint32_t>(aHeight));
+  ::CloakConfigOverlay_MergeUint(aUserContextId, "screen.width", static_cast<uint32_t>(aWidth));
+  ::CloakConfigOverlay_MergeUint(aUserContextId, "screen.height", static_cast<uint32_t>(aHeight));
 }
 
 /* static */ bool
 ScreenDimensionManager::GetDimensions(uint32_t aUserContextId, int32_t* aWidth, int32_t* aHeight) {
-  nsString widthKey, heightKey;
-  widthKey.AppendPrintf("screenWidth_%u", aUserContextId);
-  heightKey.AppendPrintf("screenHeight_%u", aUserContextId);
-
   uint32_t width = 0, height = 0;
-  RoverfoxStorageManager::GetUint(widthKey, width);
-  RoverfoxStorageManager::GetUint(heightKey, height);
+  if (auto w = MaskConfig::GetUint32("screen.width")) width = w.value();
+  if (auto h = MaskConfig::GetUint32("screen.height")) height = h.value();
 
   if (width > 0 && height > 0) {
     *aWidth = static_cast<int32_t>(width);
@@ -56,18 +50,13 @@
 
 /* static */ void
 ScreenDimensionManager::SetColorDepth(uint32_t aUserContextId, int32_t aColorDepth) {
-  nsString key;
-  key.AppendPrintf("screenColorDepth_%u", aUserContextId);
-  RoverfoxStorageManager::PutUint(key, static_cast<uint32_t>(aColorDepth));
+  ::CloakConfigOverlay_MergeUint(aUserContextId, "screen.colorDepth", static_cast<uint32_t>(aColorDepth));
 }
 
 /* static */ int32_t
 ScreenDimensionManager::GetColorDepth(uint32_t aUserContextId) {
-  nsString key;
-  key.AppendPrintf("screenColorDepth_%u", aUserContextId);
-  uint32_t depth = 0;
-  RoverfoxStorageManager::GetUint(key, depth);
-  return static_cast<int32_t>(depth);
+  if (auto d = MaskConfig::GetUint32("screen.colorDepth")) return static_cast<int32_t>(d.value());
+  return 0;
 }
 
 /* static */ nsString
--- a/b/dom/base/WebGLParamsManager.cpp	2026-04-17 18:44:43
+++ b/dom/base/WebGLParamsManager.cpp	2026-04-17 21:24:35
@@ -1,5 +1,6 @@
 /* Cloakfox: Per-context WebGL parameter manager */
 #include "WebGLParamsManager.h"
+#include "MaskConfig.hpp"
 
 #include "mozilla/dom/BrowsingContext.h"
 #include "nsGlobalWindowInner.h"
@@ -21,27 +22,29 @@
 }
 
 void WebGLParamsManager::SetVendor(uint32_t aId, const nsAString& aValue) {
-  nsAutoString key;
-  key.AppendPrintf("webgl_vendor_%u", aId);
-  RoverfoxStorageManager::PutString(key, aValue);
+  NS_ConvertUTF16toUTF8 utf8v(aValue);
+  ::CloakConfigOverlay_MergeString(aId, "webGl:vendor", std::string(utf8v.get()));
 }
 
 bool WebGLParamsManager::GetVendor(uint32_t aId, nsAString& aOut) {
-  nsAutoString key;
-  key.AppendPrintf("webgl_vendor_%u", aId);
-  return RoverfoxStorageManager::GetString(key, aOut);
+  if (auto val = MaskConfig::GetString("webGl:vendor")) {
+    aOut = NS_ConvertUTF8toUTF16(val.value().c_str());
+    return true;
+  }
+  return false;
 }
 
 void WebGLParamsManager::SetRenderer(uint32_t aId, const nsAString& aValue) {
-  nsAutoString key;
-  key.AppendPrintf("webgl_renderer_%u", aId);
-  RoverfoxStorageManager::PutString(key, aValue);
+  NS_ConvertUTF16toUTF8 utf8r(aValue);
+  ::CloakConfigOverlay_MergeString(aId, "webGl:renderer", std::string(utf8r.get()));
 }
 
 bool WebGLParamsManager::GetRenderer(uint32_t aId, nsAString& aOut) {
-  nsAutoString key;
-  key.AppendPrintf("webgl_renderer_%u", aId);
-  return RoverfoxStorageManager::GetString(key, aOut);
+  if (auto val = MaskConfig::GetString("webGl:renderer")) {
+    aOut = NS_ConvertUTF8toUTF16(val.value().c_str());
+    return true;
+  }
+  return false;
 }
 
 void WebGLParamsManager::DisableVendorFunction(uint32_t aId) {
--- a/b/dom/base/WebRTCIPManager.cpp	2026-04-17 18:44:43
+++ b/dom/base/WebRTCIPManager.cpp	2026-04-17 21:24:48
@@ -1,6 +1,7 @@
 #include "WebRTCIPManager.h"
 #include "nsTHashMap.h"
 #include "RoverfoxStorageManager.h"
+#include "MaskConfig.hpp"
 #include "nsPrintfCString.h"
 #include "nsGlobalWindowInner.h"
 #include "nsDocShell.h"
@@ -48,32 +49,38 @@
 
 /* static */ void
 WebRTCIPManager::SetIPv4(uint32_t userContextId, const nsAString& ipv4) {
-  nsString key = IPv4KeyForUserContext(userContextId);
-  RoverfoxStorageManager::PutString(key, ipv4);
-  
+  NS_ConvertUTF16toUTF8 utf4(ipv4);
+  ::CloakConfigOverlay_MergeString(userContextId, "webrtc:ipv4", std::string(utf4.get()));
+
   // Mark the function as disabled for this context after first use
   DisableIPv4Function(userContextId);
 }
 
 /* static */ bool
 WebRTCIPManager::GetIPv4(uint32_t userContextId, nsAString& outIPv4) {
-  nsString key = IPv4KeyForUserContext(userContextId);
-  return RoverfoxStorageManager::GetString(key, outIPv4);
+  if (auto val = MaskConfig::GetString("webrtc:ipv4")) {
+    outIPv4 = NS_ConvertUTF8toUTF16(val.value().c_str());
+    return true;
+  }
+  return false;
 }
 
 /* static */ void
 WebRTCIPManager::SetIPv6(uint32_t userContextId, const nsAString& ipv6) {
-  nsString key = IPv6KeyForUserContext(userContextId);
-  RoverfoxStorageManager::PutString(key, ipv6);
-  
+  NS_ConvertUTF16toUTF8 utf6(ipv6);
+  ::CloakConfigOverlay_MergeString(userContextId, "webrtc:ipv6", std::string(utf6.get()));
+
   // Mark the function as disabled for this context after first use
   DisableIPv6Function(userContextId);
 }
 
 /* static */ bool
 WebRTCIPManager::GetIPv6(uint32_t userContextId, nsAString& outIPv6) {
-  nsString key = IPv6KeyForUserContext(userContextId);
-  return RoverfoxStorageManager::GetString(key, outIPv6);
+  if (auto val = MaskConfig::GetString("webrtc:ipv6")) {
+    outIPv6 = NS_ConvertUTF8toUTF16(val.value().c_str());
+    return true;
+  }
+  return false;
 }
 
 
