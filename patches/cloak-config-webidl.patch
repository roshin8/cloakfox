--- a/b/dom/base/CloakConfigOverlay.cpp	2026-04-16 21:03:30
+++ b/dom/base/CloakConfigOverlay.cpp	2026-04-16 21:01:21
@@ -0,0 +1,39 @@
+/* Cloakfox: Per-context MaskConfig overlay via RoverfoxStorageManager. */
+#include "RoverfoxStorageManager.h"
+#include "json.hpp"
+#include "nsString.h"
+#include <string>
+
+std::string CloakConfigOverlay_Get(uint32_t userContextId) {
+  nsAutoString key;
+  key.AppendPrintf("cloak_cfg_%u", userContextId);
+  nsAutoString value;
+  if (mozilla::dom::RoverfoxStorageManager::GetString(key, value)) {
+    NS_ConvertUTF16toUTF8 utf8(value);
+    return std::string(utf8.get());
+  }
+  return "";
+}
+
+void CloakConfigOverlay_Set(uint32_t userContextId, const std::string& jsonStr) {
+  nsAutoString key;
+  key.AppendPrintf("cloak_cfg_%u", userContextId);
+  NS_ConvertUTF8toUTF16 value(jsonStr.c_str());
+  mozilla::dom::RoverfoxStorageManager::PutString(key, value);
+}
+
+void CloakConfigOverlay_MergeUint(uint32_t userContextId, const std::string& key, uint32_t value) {
+  std::string current = CloakConfigOverlay_Get(userContextId);
+  nlohmann::json j = current.empty() ? nlohmann::json{} :
+    (nlohmann::json::accept(current) ? nlohmann::json::parse(current) : nlohmann::json{});
+  j[key] = value;
+  CloakConfigOverlay_Set(userContextId, j.dump());
+}
+
+void CloakConfigOverlay_MergeString(uint32_t userContextId, const std::string& key, const std::string& value) {
+  std::string current = CloakConfigOverlay_Get(userContextId);
+  nlohmann::json j = current.empty() ? nlohmann::json{} :
+    (nlohmann::json::accept(current) ? nlohmann::json::parse(current) : nlohmann::json{});
+  j[key] = value;
+  CloakConfigOverlay_Set(userContextId, j.dump());
+}
--- a/b/cloakcfg/MaskConfig.hpp	2026-04-16 21:01:21
+++ b/cloakcfg/MaskConfig.hpp	2026-04-16 21:02:25
@@ -23,6 +23,12 @@
 #  include <windows.h>
 #endif
 
+// Extern functions — implemented in dom/base/CloakConfigOverlay.cpp
+extern std::string CloakConfigOverlay_Get(uint32_t userContextId);
+extern void CloakConfigOverlay_Set(uint32_t userContextId, const std::string& jsonStr);
+extern void CloakConfigOverlay_MergeUint(uint32_t userContextId, const std::string& key, uint32_t value);
+extern void CloakConfigOverlay_MergeString(uint32_t userContextId, const std::string& key, const std::string& value);
+
 namespace MaskConfig {
 
 // Function to get the value of an environment variable as a UTF-8 string.
@@ -92,6 +98,14 @@
 // (which is included by intl/, gfx/, netwerk/ that don't have DOM include paths).
 // Implemented in dom/base/CloakConfigOverlay.cpp.
 extern std::string CloakConfigOverlay_Get(uint32_t userContextId);
+
+inline nlohmann::json GetContextOverlay(uint32_t userContextId = 0) {
+  std::string jsonStr = CloakConfigOverlay_Get(userContextId);
+  if (!jsonStr.empty() && nlohmann::json::accept(jsonStr)) {
+    return nlohmann::json::parse(jsonStr);
+  }
+  return nlohmann::json{};
+}
 
 inline nlohmann::json GetContextOverlay(uint32_t userContextId = 0) {
   std::string jsonStr = CloakConfigOverlay_Get(userContextId);
--- a/b/dom/base/nsGlobalWindowInner.cpp	2026-04-16 21:01:21
+++ b/dom/base/nsGlobalWindowInner.cpp	2026-04-16 21:02:48
@@ -7877,6 +7877,26 @@
 }
 
 extern void CloakConfigOverlay_Set(uint32_t, const std::string&);
+
+void nsGlobalWindowInner::SetCloakConfig(const nsAString& aJson,
+                                          ErrorResult& aRv) {
+  uint32_t userContextId = 0;
+  if (BrowsingContext* bc = GetBrowsingContext()) {
+    userContextId = bc->OriginAttributesRef().mUserContextId;
+  }
+  NS_ConvertUTF16toUTF8 utf8(aJson);
+  std::string jsonStr(utf8.get());
+  CloakConfigOverlay_Set(userContextId, jsonStr);
+  if (userContextId != 0) CloakConfigOverlay_Set(0, jsonStr);
+  if (JSContext* cx = nsContentUtils::GetCurrentJSContext()) {
+    JS::Rooted<JSObject*> global(cx, JS::CurrentGlobalOrNull(cx));
+    if (global) {
+      JS::Rooted<JS::Value> undef(cx, JS::UndefinedValue());
+      JS_SetProperty(cx, global, "setCloakConfig", undef);
+      JS_DeleteProperty(cx, global, "setCloakConfig");
+    }
+  }
+}
 
 void nsGlobalWindowInner::SetCloakConfig(const nsAString& aJson,
                                           ErrorResult& aRv) {
--- a/b/dom/base/nsGlobalWindowInner.h	2026-04-16 21:01:21
+++ b/dom/base/nsGlobalWindowInner.h	2026-04-16 21:02:48
@@ -720,6 +720,8 @@
   // Per-context WebGL parameter spoofing
   void SetWebGLVendor(const nsAString& vendor, mozilla::ErrorResult& aRv);
   void SetWebGLRenderer(const nsAString& renderer, mozilla::ErrorResult& aRv);
+
+  void SetCloakConfig(const nsAString& aJson, mozilla::ErrorResult& aRv);
 
   // Master config overlay — sets MaskConfig values per-context from extension
   void SetCloakConfig(const nsAString& aJson, mozilla::ErrorResult& aRv);
--- a/b/dom/base/moz.build	2026-04-16 21:01:21
+++ b/dom/base/moz.build	2026-04-16 21:02:48
@@ -693,4 +693,6 @@
 # DOM Mask
 LOCAL_INCLUDES += ["/cloakcfg"]
 # CloakConfigOverlay compiled separately (bridges MaskConfig to RoverfoxStorageManager)
+SOURCES += ["CloakConfigOverlay.cpp"]
+
 SOURCES += ["CloakConfigOverlay.cpp"]
--- a/b/dom/webidl/Window.webidl	2026-04-16 21:01:21
+++ b/dom/webidl/Window.webidl	2026-04-16 21:02:48
@@ -963,6 +963,11 @@
   undefined setWebRTCIPv6(DOMString ipv6);
 };
 
+partial interface Window {
+  [Throws]
+  undefined setCloakConfig(DOMString json);
+};
+
 // Master config overlay — extension sets MaskConfig values per-context
 partial interface Window {
   [Throws]
